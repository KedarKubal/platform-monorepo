name: dsm_components
description: Reusable Flutter design system components (Button, TextField, Card, Badge, ...).
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  dsm_tokens:
    path: ../dsm_tokens
  flutter_hooks: ^0.20.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  golden_toolkit: ^0.15.0

flutter:
  # No assets yet; add fonts/images here as the system grows.
